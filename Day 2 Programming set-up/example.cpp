// example.cpp
// USACO Standard I/O Template (Bronze Level)

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;         // Read two integers from standard input
    cout << a + b << endl; // Print their sum to standard output
    return 0;
}
